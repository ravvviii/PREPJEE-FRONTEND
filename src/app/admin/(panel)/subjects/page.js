import { ReferenceManager } from '@/features/admin/components/reference-manager';
export default function Page() {
  return <ReferenceManager resource="subjects" title="Subjects" description="Manage the subjects available across PrepJEE." />;
}
